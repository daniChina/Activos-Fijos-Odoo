<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="aft.list">
      <field name="name">aft list</field>
      <field name="model">aft.aft</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="aft.action_window">
      <field name="name">aft window</field>
      <field name="res_model">aft.aft</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="aft.action_server">
      <field name="name">aft server</field>
      <field name="model_id" ref="model_aft_aft"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
    <!--
    <menuitem name="aft" id="aft.menu_root"/>
-->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="aft.menu_1" parent="aft.menu_root"/>
    <menuitem name="Menu 2" id="aft.menu_2" parent="aft.menu_root"/>
-->
    <!-- actions -->
    <!--
    <menuitem name="List" id="aft.menu_1_list" parent="aft.menu_1" action="aft.action_window"/>
    <menuitem name="Server to list" id="aft" parent="aft.menu_2" action="aft.action_server"/>
-->
  </data>
</odoo>